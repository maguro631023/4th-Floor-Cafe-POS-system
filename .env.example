# PostgreSQL (Railway 會提供 DATABASE_URL)
DATABASE_URL="postgresql://user:password@host:5432/dbname?schema=public"

# 登入 session 加密用，至少 32 字元（正式環境請自訂）
SESSION_SECRET="change-me-in-production-min-32-chars!!"
